<?xml version="1.0" encoding="utf-8"?>
<android.support.wearable.view.BoxInsetLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fragment_silence_root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:id="@+id/fragment_silence_textview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="4dp"
            android:layout_marginTop="16dp"
            android:gravity="top|center_horizontal"
            android:text="@string/silence_application"
            android:textAlignment="center"
            android:textAllCaps="true" />

        <android.support.wearable.view.DelayedConfirmationView
            android:id="@+id/fragment_silence_delayedView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center|center_horizontal"
            android:src="@drawable/ic_action_send"
            app:circle_border_color="@color/green"
            app:circle_border_width="16dp"
            app:circle_color="@color/red"
            app:circle_padding="16dp"
            app:circle_radius="60dp"
            app:update_interval="150" />

        <ScrollView
            android:id="@+id/fragment_silence_scrollview"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <LinearLayout
                android:id="@+id/fragment_silence_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@android:color/transparent"
                android:orientation="vertical" >

            </LinearLayout>

        </ScrollView>

    </LinearLayout>

</android.support.wearable.view.BoxInsetLayout>